import { Track } from './track';

export class Playlist {
    duration_ms?: number;
    name?: string;
    num_albums?: number;
    num_artists?: number;
    num_followers?: number;
    num_tracks?: number;
    pid?: number;
}